# 架构重构总结

> 这是历史记录文档，只保留已经发生的重构过程、变更摘要和阶段性结论。
>
> 主文档请看 `Docs/框架现状与修理计划.md`。
>
> 本文件不承担当前计划、待办排期或风险判断职责。

## 最近重构记录

### 2026-07-24 UI 框架架构清理

1. **OperationBus 线程安全**：`s_activeInstance` 改为 `std::atomic<OperationBus*>`，消除潜在数据竞争
2. **RenderWidget 线程安全**：`s_nextEntityId` 改为 `std::atomic<uint64_t>`，使用 `fetch_add` 替代 `++`
3. **删除重复视口**：移除已废弃的 `Viewport2D`（QGraphicsView）和 `MouseInteractionAdapter`，生产路径统一使用 `RenderViewport2D`
4. **删除死代码**：
   - `UI/2D/Service/ServiceLocator.h/.cpp`（已废弃，无调用者）
   - `UI/2D/Service/ServiceCollection2D.h`（已废弃，无调用者）
   - `UI/2D/Operation/CommandActionHub.h/.cpp`（已排除编译）
5. **RenderWidget 清理**：删除6个零调用 deprecated 方法（addScreenText, addWorldAnchorText, setMouseCoordinateDisplay, setMeasurementText, updateSceneOnDevice），移除4个无用成员变量（m_drawingColor, m_bAntialiasing, m_bWireframeMode, m_bDepthTest）
6. **C ABI facade 清理**：删除6个模块的 C API 文件（Log/SyLoggerDLL.h, Vision/VisionDLL.h/.cpp, GeoModelCore/GeoModelDLL.cpp, Engraving/EngravingCAPI.h/.cpp, FileIO/FileIOExport.h/.cpp），精简 EngineAPI.h 和 SyLogger.h

### 2026-07-26 UI 视口与组合根继续收口

1. **Camera2D 独立化**：将内嵌在 `RenderViewport2D` 中的 `Camera2D` 结构及其全部方法提取到 `Main/Src/UI/Camera2D.h/.cpp`，视口类减少约 130 行
2. **选择逻辑独立化**：将 `performHitTest`、`beginBoxSelect`、`updateBoxSelect`、`endBoxSelect`、`handleLeftClick` 从 `RenderViewport2D` 提取到 `Main/Src/UI/ViewportSelector.h/.cpp`，并移除 box-select 状态变量，视口类减少约 260 行
3. **文件操作注册收口**：将 `registerFileOperations()` 及其辅助函数从 `ApplicationCompositionRoot` 提取到 `Main/Src/Composition/FileOperationRegistry.h/.cpp`
4. **核心操作注册收口**：将 `registerCoreOperations()` 从 `ApplicationCompositionRoot` 提取到 `Main/Src/Composition/CoreOperationRegistry.h/.cpp`
5. **废弃路径清理**：移除 `UiWorkbench.cpp` 中对已废弃 `RenderViewportAdapter.h` 的引用，并删除 `SceneBuilder2D.cpp/.h`、`RenderViewportAdapter.cpp/.h`、`SceneEditServiceAdapter.cpp/.h` 等未使用文件
6. **组合根瘦身**：`ApplicationCompositionRoot` 从约 836 行降至约 380 行，进一步回归“装配与生命周期管理”职责

### 2026-07-22 架构清理

1. **OperationBus 合并**：移除 `ServiceLocator` 中的重复 `OperationBus` 实例，全局统一使用 `ApplicationCompositionRoot` 创建的单例
2. **死代码清理**：删除 `ICommandHandler` 接口（`UiCommandManager.h/cpp`）和 `CommandHandlerAdapter` 引用
3. **SceneManager 解耦**：Engine2D 算法头文件（`INestingJobRunner.h`、`NestingPartPreparer.h`、`EntityBoolean.h`、`EntityOffset.h`）改用前向声明，不再 include 具体 `SceneManager.h`
4. **RenderX 边界修复**：移除 `Main/CMakeLists.txt`、`UI/2D/CMakeLists.txt`、`UI/3D/CMakeLists.txt`、`UI/RenderCompat/CMakeLists.txt` 中对 `${SANYI_RENDERX_DIR}/src` 的引用；Renderx 改用 `target_link_libraries(Log)` 替代 `include_directories/link_directories`

---

## 当前架构（历史上下文）

阶段记录中保留 **SyEntity 多态模型 + Protobuf 序列化** 方案，EntityRecord 相关的新架构尝试已移除。

> 说明：这一段是写给“当时如何收口”的历史背景，不是当前架构权威描述。当前主线请看 `Docs/框架现状与修理计划.md`。

---

## 核心数据模型（阶段上下文）

### SyEntity 多态体系

所有图元继承自 `SyEntity` 基类，支持多态操作：

- **简单图元**：SyLine、SyCircle、SyArc、SyPoint
- **复合图元**：SyPolygon、SySmartLine（包含多个子图元）
- **曲线**：SyBezier、SyBezier2、SyNurbs
- **特殊图元**：SyText、SyImage、SyQRCode、SyBarCode

### Protobuf 序列化

文件 I/O 使用 Protobuf 二进制格式，支持完整的实体类型体系：

- 支持所有 SyEntity 派生类型的序列化/反序列化
- 支持 SmartLine 等复合图元的嵌套序列化
- 支持图层、群组、文档元数据的完整存储

---

## 现有模块结构（历史上下文）

| 模块 | 职责 |
|------|------|
| `Engine/Common` | SyEntity 基类、图层、通用图元接口 |
| `Engine/2D` | 2D 几何引擎、图元实现、编辑服务 |
| `Engine/3D` | 3D 几何引擎、网格、BRep |
| `FileIO` | 文件导入导出、Protobuf 序列化 |
| `UI/2D` | 2D 视图、工具系统、OperationBus |
| `UI/3D` | 3D 视图、场景树、OperationBus3D |
| `UI/Common` | 共享 UI 基础设施（OperationBusBase, CommandCatalog） |
| `UI/RenderCompat` | RenderWidget 渲染兼容层 |
| `Renderx` | 渲染管线、Shader、OpenGL 后端 |
| `Main` | 主窗口、工作台、应用组装 |

---

## 验证清单（历史）

- [ ] 所有单元测试通过
- [ ] DXF/DWG/SVG 导入导出正常
- [ ] 撤销/重做功能正常
- [ ] 图层锁定/可见性生效
- [ ] 选择/拖拽/编辑无崩溃
- [ ] 渲染帧率 ≥ 60fps (10k 图元)
- [ ] 内存占用无泄漏
- [ ] 文档保存/加载往返一致

---

## 后续优化方向（历史记录）

本节保留的是阶段记录当时的判断，不作为当前计划使用。当前开发请看 `Docs/框架现状与修理计划.md`。

1. **命令系统统一**：✅ 已完成 — `ICommandHandler` 接口已删除，`CommandHandlerAdapter` 已移除，所有命令统一走 `OperationBus`
2. **视口职责继续收口**：继续将 `RenderViewport2D` 中残留的刷新协调、输入分发和生命周期细节分层，避免新逻辑回流到视口壳
3. **选择与交互进一步解耦**：持续收紧 `ViewportSelector` 的边界，确保它只负责选择，不吸收工具、命令或刷新职责
4. **事务与 Undo 统一**：将 Undo/Redo 绑定到事务回放
5. **文档与编辑服务统一**：收敛文档事实源
6. **渲染路径收口**：✅ 已完成 — `Renderx/` 为唯一生产路径，旧路径已删除
7. **空壳模块清理**：移除 Network/Hardware/PythonHost 等空壳模块
8. **SceneManager 接口扩展**：将 `addEntities`/`deleteEntitiesBatch` 等 2D 专用方法下沉到 `ISceneManager` 接口或专用接口
9. **OperationContext 接口化**：将 `OperationContext` 中的 `MainWindow*` 替换为 `IMainWindow` 抽象接口，使 Operation 可复用于 3D 和测试场景
10. **删除 MainWindow 空壳**：在 `OperationContext` 接口化完成后移除 UI/2D 的空壳 `MainWindow` 实现