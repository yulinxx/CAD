# 框架现状与修理计划

本文是当前开发主文档，只保留当前状态、当前问题和后续计划，不保留历史阶段记录。

---

## 1. 当前状态

### 1.1 总判断

框架已经完成主要收口，当前重点不再是回顾阶段过程，而是继续把剩余边界收紧，并保持文档与代码同步。

### 1.2 当前稳定项

- 主渲染路径统一到 `Renderx` / `SanYiRender`
- 场景事实源仍以 `SceneManager` 为准
- `SyEntity` 多态模型 + Protobuf 仍是当前稳定主干
- `Camera2D` 和 `ViewportSelector` 已从视口中拆出
- `FileOperationRegistry` 和 `CoreOperationRegistry` 已从组合根中抽离
- 增量渲染接口已存在，刷新语义已统一
- 3D 工具、菜单、状态栏和渲染适配层已具备当前生产链路

### 1.3 当前风险项

- 文档中仍可能残留旧模块名、旧路径和旧历史术语
- 3D 兼容层与主链之间仍需要继续收口
- 部分历史分析文档仍需改写为“现状 + 计划”形式
- 少量 ABI 细节仍需继续统一版本查询和错误码风格

---

## 2. 当前工作重点

1. 清理所有文档中的旧历史叙述。
2. 统一当前模块名和当前目录名。
3. 继续压缩 UI 与实现细节之间的耦合。
4. 继续统一 C ABI 风格。
5. 继续保持渲染主路径只剩 `Renderx` / `SanYiRender`。

---

## 3. 当前需要继续做的事

- 继续收紧 3D 兼容层和主链边界
- 继续统一各 facade 的版本查询和错误码
- 继续清理历史分析文档中的旧路径
- 继续把计划性文档和现状性文档分开

---

## 4. 当前结论

本文后续只接受两类内容：

- 当前已确认的现状
- 后续待做的计划

不再保留阶段总结、时间线记录或历史回顾章节。
