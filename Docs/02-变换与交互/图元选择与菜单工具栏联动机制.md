# 图元选择与菜单/工具栏联动机制

## 1. 概述

本文档描述 2D 工作台中，当用户选择（或取消选择）图元时，菜单栏和顶部工具栏的按钮启用状态如何实时联动的技术架构与实现流程。

---

## 2. 核心概念

### 2.1 命令中枢 (CommandActionHub)

`CommandActionHub` 是整个 UI 命令系统的核心枢纽，负责：
- 托管所有命令的 QAction（菜单项、工具栏按钮）
- 维护命令启用状态的单一事实来源 (`CommandUiSnapshot`)
- 处理命令分发（点击后执行什么操作）

### 2.2 命令 UI 快照 (CommandUiSnapshot)

`CommandUiSnapshot` 是选择状态的单一事实来源，包含：

```cpp
struct CommandUiSnapshotBase
{
    bool hasSelection{ false };      // 是否有选中图元
    int selectionCount{ 0 };         // 选中的图元数量
    bool hasClipboard{ false };       // 剪贴板是否有内容
    bool canUndo{ false };           // 撤销栈是否可撤销
    bool canRedo{ false };           // 重做栈是否可重做

    // 锁定维度
    bool anyLockedLayer{ false };    // 选中的图元是否位于锁定图层
    bool anyLockedEntity{ false };   // 选中的图元本身是否被锁定
    bool anyEditable{ false };       // 是否有可编辑的图元
    bool anyHidden{ false };         // 是否有隐藏的图元

    // 类型直方图（用于工具栏上下文切换）
    uint32_t typeMask{ 0 };

    // 辅助方法
    bool anyLocked() const { return anyLockedLayer || anyLockedEntity; }
};
```

### 2.3 命令目录 (CommandCatalog)

`CommandCatalog` 定义了所有命令的元数据，包括：
- 命令 ID（如 `edit.delete`、`edit.mirror_horizontal`）
- 启用规则 (`CommandEnable2D`)

```cpp
enum class CommandEnable2D : uint8_t
{
    Always = 0,                      // 始终可用
    RequiresSelection = 1,           // 需要有选中图元
    RequiresMultiSelection = 2,      // 需要多选（>=2）
    RequiresClipboard = 3,           // 需要剪贴板有内容
    RequiresUndo = 4,                // 需要可撤销
    RequiresRedo = 5,                // 需要可重做
    RequiresUnlockedSelection = 6,   // 需要选中且未锁定
    RequiresUnlockedMultiSelection = 7, // 需要多选且未锁定
    RequiresVisibleSelection = 100,  // 需要选中且可见（无锁定无隐藏）
    RequiresVisibleMultiSelection = 101   // 需要多选且可见
};
```

---

## 3. 联动流程图

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           用户交互层                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  菜单栏      │  │  顶部工具栏  │  │  左侧工具栏  │  │  右键菜单   │   │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘   │
└─────────┼────────────────┼────────────────┼────────────────┼───────────────┘
          │                │                │                │
          ▼                ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        CommandActionHub (命令中枢)                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  m_editActions    - 普通编辑命令 QAction 映射                       │   │
│  │  m_toggleEditActions - 可切换命令 QAction 映射                      │   │
│  │  m_toolActions   - 绘图工具 QAction 映射                            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  refreshActionStates(snapshot)                                       │   │
│  │    ├── refreshEditActionStates()  → 刷新普通命令按钮                 │   │
│  │    ├── refreshToggleActionStates() → 刷新切换命令按钮               │   │
│  │    └── emit selectionContextChanged(snapshot)                        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │
          ┌───────────────────────────┼───────────────────────────┐
          │                           │                           │
          ▼                           ▼                           ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│ Workbench2D     │     │ 属性面板        │     │ 状态栏          │
│ applySelection  │     │ refreshProperties│     │ setSelectionInfo│
│ Context(snapshot)│     │                 │     │                 │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

### 3.2 按钮启用状态刷新流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  触发源（任一发生即刷新）                                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐        │
│  │ 用户选择图元      │  │ 锁定/解锁图层     │  │ 撤销/重做操作    │        │
│  │ selectionChanged │  │ sigLayerChanged  │  │ undoStateChanged │        │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘        │
└───────────┼────────────────────┼────────────────────┼──────────────────────┘
            │                    │                    │
            └────────────────────┼────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  UiStateBridge2D::install()  - 统一连接所有刷新触发源                     │
│                                                                             │
│  viewport->selectionChanged   → refreshAll(workbench)                      │
│  layerBridge->sigLayerChanged → refreshAll(workbench)                      │
│  bus->undoStateChanged       → refreshAll(workbench)                       │
│  bus->operationCompleted     → refreshAll(workbench)                        │
│  sceneMonitor->sceneChanged  → refreshAll(workbench)                       │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Workbench2D::refreshCommandUiState()                                      │
│                                                                             │
│  m_commandHub->refreshActionStates(                                        │
│      m_commandHub->captureSnapshot(m_mainWindow)                            │
│  )                                                                          │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  CommandActionHub::captureSnapshot() - 抓取当前状态快照                      │
│                                                                             │
│  1. 通过 m_selectionContextProvider 获取 SelectionContext                   │
│     └── 遍历选中图元，计算 hasSelection / anyLocked / anyHidden 等         │
│                                                                             │
│  2. 通过 m_undoRedoProvider 获取撤销/重做状态                              │
│                                                                             │
│  3. 通过 m_clipboardProvider 获取剪贴板状态                                 │
│                                                                             │
│  返回 CommandUiSnapshot{hasSelection, canUndo, canRedo, ...}               │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  CommandActionHub::refreshActionStates(snapshot)                            │
│                                                                             │
│  1. refreshEditActionStates(snapshot)                                      │
│     └── 遍历 CommandCatalog 中的所有非 checkable 命令                       │
│         └── 对每个命令：                                                    │
│             - 通过 editAction(menuId) 获取 QAction                         │
│             - 调用 applySnapshotToAction(action, snapshot)                 │
│                 └── 根据 enableRule 计算 enabled:                           │
│                     - Always → true                                        │
│                     - RequiresSelection → snapshot.hasSelection             │
│                     - RequiresVisibleSelection →                           │
│                       snapshot.hasSelection && !snapshot.anyLocked() &&     │
│                       !snapshot.anyHidden                                   │
│                     - RequiresClipboard → snapshot.hasClipboard            │
│                     - RequiresUndo → snapshot.canUndo                       │
│                     - RequiresRedo → snapshot.canRedo                       │
│                                                                             │
│  2. refreshToggleActionStates(snapshot)                                   │
│     └── 遍历所有 checkable 命令（如 Group）                                 │
│         └── 调用 resolveToggleUiState() 获取 enabled                        │
│                                                                             │
│  3. emit selectionContextChanged(snapshot)                                 │
│     └── 通知所有订阅者刷新 UI                                               │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  菜单/工具栏按钮更新                                                       │
│                                                                             │
│  QAction::setEnabled(enabled) → 按钮变灰/变亮                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 关键代码路径

### 4.1 状态提供器注入

在 `Workbench2D::createToolbars()` 中：

```cpp
// 1. 注入选择上下文提供器
m_commandHub->setSelectionContextProvider([selectionService = m_services.selectionService,
                                         layerManager = m_services.layerManager,
                                         sceneEditService = m_services.sceneEditService]() -> SelectionContext {
    // 遍历选中图元，计算 selectionCount / anyLockedLayer / anyLockedEntity / anyHidden
    SelectionContext result;
    // ...
    result.hasSelection = result.selectionCount > 0;
    result.groupEnabled = result.hasSelection && !(result.anyLockedLayer || result.anyLockedEntity || result.anyHidden);
    return result;
});

// 2. 注入撤销/重做状态提供器
m_commandHub->setUndoRedoProvider([undoManager = m_services.undoManager]() -> UndoRedoState {
    UndoRedoState state;
    if (undoManager) {
        state.canUndo = undoManager->canUndo();
        state.canRedo = undoManager->canRedo();
    }
    return state;
});

// 3. 注入剪贴板状态提供器
m_commandHub->setClipboardProvider([clipboard = m_services.clipboard]() -> bool {
    return clipboard && clipboard->hasContent();
});
```

### 4.2 按钮创建与 commandId 设置

`createEditAction()` 必须设置 `commandId` 属性，否则 `applySnapshotToAction()` 会跳过该按钮：

```cpp
QAction* CommandActionHub::createEditAction(const CommandEntry2D& entry)
{
    QAction* action = new QAction(parentWidget);
    action->setText(...);
    action->setData(static_cast<int>(entry.menuId));

    // 关键：必须设置 commandId 属性！
    if (entry.shortcutId)
    {
        const QString commandId = QString::fromUtf8(entry.shortcutId);
        action->setProperty("commandId", commandId);
        action->setObjectName(commandId);
    }
    // 如果 shortcutId 为空，从 menuId 派生
    else
    {
        const QString menuIdName = QMetaEnum::fromType<UI::MenuActionId>().valueToKey(static_cast<int>(entry.menuId));
        if (menuIdName.startsWith("Edit_"))
        {
            QString base = menuIdName.mid(5);
            base[0] = base[0].toLower();
            commandId = "edit." + base;
        }
    }
    // ...
}
```

### 4.3 启用规则判断

`applySnapshotToAction()` 中的规则判断：

```cpp
void CommandActionHub::applySnapshotToAction(QAction* action, const CommandUiSnapshot& snapshot)
{
    // 从 commandId 反查目录条目
    const QString commandId = action->property("commandId").toString();
    const UI::MenuActionId menuId = CommandCatalog::menuIdForCommandId(commandId);
    const CommandEntry2D* entry = CommandCatalog::findByMenuId(menuId);

    // 根据 enableRule 计算 enabled
    if (entry->enableRule == CommandEnable2D::RequiresVisibleSelection)
    {
        const bool enabled = snapshot.hasSelection && !snapshot.anyLocked() && !snapshot.anyHidden;
        action->setEnabled(enabled);
        return;
    }
    if (entry->enableRule == CommandEnable2D::RequiresVisibleMultiSelection)
    {
        const bool enabled = snapshot.selectionCount >= 2 && !snapshot.anyLocked() && !snapshot.anyHidden;
        action->setEnabled(enabled);
        return;
    }
    // ... 其他规则
}
```

---

## 5. 刷新触发源

所有 UI 刷新都通过 `UiStateBridge2D::install()` 统一挂载：

| 触发源 | 信号 | 刷新动作 |
|--------|------|----------|
| 用户选择图元 | `RenderViewport2D::selectionChanged` | 刷新所有按钮 |
| 锁定/解锁图层 | `QtLayerManagerBridge::sigLayerChanged` | 刷新锁定相关按钮 |
| 撤销/重做 | `OperationBus::undoStateChanged` | 刷新撤销/重做按钮 |
| 操作完成 | `OperationBus::operationCompleted` | 刷新所有按钮 |
| 场景变化 | `SceneMonitor::sceneChanged` | 刷新所有按钮 |

---

## 6. 架构设计要点

### 6.1 单一事实来源

整个 UI 状态管理的核心是 `CommandUiSnapshot`：
- 所有状态信息（hasSelection、canUndo、canRedo 等）都从快照获取
- 快照由状态提供器（Provider）捕获，而非直接从场景读取
- 这种设计使得状态计算逻辑集中，便于维护和测试

### 6.2 命令目录驱动

`CommandCatalog` 是元数据的集中定义点：
- 每个命令的启用规则在此统一定义
- 新增命令只需在目录中添加条目，无需修改刷新逻辑
- enableRule 枚举扩展性好，可根据需求添加新规则

### 6.3 统一连接层

`UiStateBridge2D` 作为桥接层：
- 将分散的信号源（selectionChanged、sigLayerChanged、undoStateChanged 等）统一管理
- 避免各 UI 组件各自监听信号导致的重复和遗漏
- 刷新请求统一流向 `Workbench2D::refreshCommandUiState()`

### 6.4 属性驱动查找

工具栏按钮通过 `commandId` 属性查找对应的 QAction：
- 菜单和工具栏的按钮在逻辑上视为一体
- 点击菜单项和点击工具栏按钮执行相同的命令
- 这种设计简化了菜单/工具栏同步问题

---

## 7. 相关文件索引

| 文件 | 职责 |
|------|------|
| `CommandCatalog.cpp` | 命令目录定义，包含所有命令的 enableRule |
| `CommandActionHubActions2D.cpp` | 命令中枢实现，负责按钮创建和状态刷新 |
| `UiWorkbench.cpp` | 工作台实现，注入状态提供器 |
| `UiStateBridge2D.cpp` | 统一连接所有刷新触发源 |
| `TopToolBar.cpp` | 顶部工具栏，解析 commandId 获取中枢 QAction |
| `WorkbenchMenuManager.cpp` | 菜单管理，刷新菜单项启用状态 |
| `CommandUiSnapshotBase.h` | 快照数据结构定义 |
| `CommandActionHubBase.h` | enableRule 判断逻辑 |

---

## 8. 测试建议

1. **无选择时**：所有需要选择的命令（Delete/Mirror/Copy/Align/Group）应该灰
2. **单选时**：Delete/Mirror/Copy 可用，Align 需要多选所以灰
3. **多选时**：所有编辑命令都可用
4. **选中锁定图层图元时**：相关命令变灰
5. **选中隐藏图元时**：相关命令变灰（RequiresVisibleSelection 规则）
6. **撤销栈非空时**：撤销按钮变亮
7. **重做栈非空时**：重做按钮变亮
8. **剪贴板有内容时**：粘贴按钮变亮

---

*本文档最后更新：2026-09-11*
