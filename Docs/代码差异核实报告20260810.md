# 代码差异核实报告 2026-08-10

> 状态：核实结论，作为当前代码事实的权威参照。
>
> 方法：五路并行核查（文件结构 + grep 引用 + 关键实现阅读），以当前工作树代码为准。
>
> 依据：本文档修正了 `框架现状与修理计划.md`、`框架问题分析DS20260729.md`、`Renderx/README.md`、`RenderX问题分析OpenCodeLing20260805.md`、`当前架构总览.md` 中过时或被证伪的记录。

> **2026-08-11 复核追加**：`WorkbenchMenuManager` 中 `refreshEditMenuForWorkbench` / `refreshModifyMenuForWorkbench` / `refreshAlgorithmMenuForWorkbench` 的 3D 死分支（`#if BUILD_UI3D` 下 `CommandCatalog3D` / `MenuActionId3D` 过滤）已全部删除，文件现**仅服务 2D**；三个函数对 `3D` 工作台早退（`SY_DEBUGF` 后 return）。清空逻辑由 `qDeleteAll` 改为 `clearMenuActions()`（连带销毁 Rotate/Mirror/Align/Path Operations 等子菜单对象，修复孤儿 `QMenu` 泄漏）。文件行数 1565 → **1484 行**，与本文档 §2、§5 对照可见。详见 `菜单功能完善计划.md` 12.2 与 `菜单工具栏交互.md` 4.3。

> **2026-08-11 追加②**：旧 `UI/2D` `MenuManager` 子系统（`Src/Ui/MenuManager/` 共 20 文件）已整体删除——删除前核查确认从未实例化（`new MenuManager(` 无调用）、目录不在 UI2D 编译 GLOB 清单内、全部引用者本未参与编译，且 `BaseMenu`（3D 复用）与 `CommandRegistry3D`（3D）不受影响；`UI2D` DLL 删除后编译通过。§5 的"三套并存"据此更新为"已删其一"。

## 0. 结论速览

| # | 验证项 | 判定 | 关键差异 |
|---|---|---|---|
| 1 | 操作注册外移 | ✅ 已完全外移 | 组合根 0 处直接注册；旧 `EditOperations.cpp` 已删除，操作按新框架接入 `CoreOperationRegistry`/`AlgorithmRunner`/`ViewportActionHub` |
| 2 | WorkbenchWindow 管理器化 | ✅ 已实质完成 | 推翻"三 Manager 已删 / 窗口 God 类"旧记录 |
| 3 | 多窗口 + Vulkan/Metal | ✅ 已核实并修复 | 多实例设备；Vulkan 后端已编译通过（装 SDK 后修复 ~70 处错误），Metal 待 macOS 验证 |
| 4 | SceneEditService + 导入导出 | ⚠️ 大部分真实现 | 发现 PDF/BMP/PNG/STEP 导出坏链、OBJ/ImportImage 空壳、DWG 缺失 |
| 5 | 菜单系统 | ⚠️ 未达 CommandCatalog 愿景 | 仅 Draw 菜单动态生成；配置驱动 UI 默认关闭；refresh 系列已收敛为 2D 专用（3D 由 MenuManager3D 自理） |

## 1. 操作注册外移 — 已完全外移

**代码事实**（`Main\Src\Composition\`）：
- `ApplicationCompositionRoot.cpp` 中直接 `registerOperation` 调用数 = **0**
- `registerAllOperations()`（L266-301）是纯编排壳，仅委托三个 Registry：
  - `FileOperationRegistry`：17 个文件操作
  - `CoreOperationRegistry`：编辑/算法/视图/帮助操作（剪贴板、变换、群组、修剪延伸、贝塞尔、阵列、离散化、包围盒、算法分发、视图缩放/网格/捕捉/图层/单位、浮雕）
  - `PendingOperationRegistry`：已无占位操作（2026-08-14 视图占位全部接入后保留空实现）
- `ApplicationCompositionRoot` 构造函数为 ~10 行入口，拆分为 `assembleUiServices` / `setupImportExportServices` / `setupDialogServices` / `setupDirtyStateSync` / `registerAllOperations` 五个子方法

**残留问题**：
- ✅ `EditOperations.cpp`（26 操作）已物理删除（2026-08-14 清理），全部按新框架重写接入：选择/剪贴板/变换/群组/修剪延伸/贝塞尔/阵列/离散化/包围盒由 `CoreOperationRegistry` 以 `LambdaOperation` 注册，算法（Fill/Nesting/Offset/Array/Boolean/Relief）经 `AlgorithmRunner` 分发，视图缩放经 `ViewportActionHub` 分发。
- 🟡 部分旧文档仍写 `registerPendingAlgorithmOperations`，实际类名 `PendingOperationRegistry`（已在主文档修正）。

## 2. WorkbenchWindow 管理器化 — 已实质完成

**旧记录**（`框架问题分析DS20260729.md` L132 / v1.11 版本记录）：
> 原始 1392 行已降至 1244 行；三 Manager 作为死代码从未接线，已于 v1.11 删除（共 6 文件 726 行）。

**代码事实**：
- `WorkbenchWindow.cpp` = **1015 行**，主体为单行转发委托
- 四个 Manager **全部存在并接线**（`WorkbenchWindow.cpp` 构造时实例化，全部编入 CMake）：
  - `WorkbenchMenuManager`（1484 行，2026-08-11 删除 3D 死分支后）
  - `WorkbenchLayoutManager`（427 行）
  - `WorkbenchActionManager`（88 行）
  - `WorkbenchStateManager`（420 行）
- 视口由工作台创建注入（`Workbench2D::createCentralViewport`，`UiWorkbench.cpp:309`），窗口不 new 视口
- 窗口保留职责：构造函数装配、closeEvent、`triggerWorkbench` 编排、最近文件持久化
- 创建链：`AppBootstrapper → ApplicationCompositionRoot → UiShellHost → WorkbenchWindow`

**结论**：v1.11"删除三 Manager"的记录与代码矛盾（或 Manager 被后续重新引入）。以当前代码为准：管理器化已落地。

## 3. 多窗口 + Vulkan/Metal — 代码超前于文档

**代码事实**（`Renderx\src\rhi\`）：
- `renderCreateDevice` 支持任意多实例，无单例限制；Null 后端测试含 `TwoDevicesAreIsolated`
- **Vulkan 1725 行**：完整 Vulkan 1.2 实现（swapchain、管线、indirect draw、compute dispatch、内存预算），仅 1 处 TODO
- **Metal 1076 行**：真实 MTL API 调用，零 TODO
- **Null 249 行**：软件后端
- 条件编译属实：`Renderx\CMakeLists.txt:91-116`，`find_package(Vulkan QUIET)` 找到才编，无 SDK 自动跳过

**⚠️ 2026-08-10 编译验证（用户安装 Vulkan SDK 1.4.357 后）**：
- 初始编译暴露 ~70 处错误，其中多数为**真实 API 语义误用**，证明该后端从未被真实编译过（旧文档 M9"编译通过"断言失实）：
  - `VK_NO_EXPOSED_TYPES` 宏遮蔽全部 Vulkan 扩展类型
  - `VkSwapchainCreateInfoKHR` 错用 `colorSpace`/`extent`（应为 `imageColorSpace`/`imageExtent`）
  - `VkPipelineVertexInputStateCreateInfo` 错用 `pVertexInputBindingDescriptions`（应为 `pVertexBindingDescriptions`）
  - `VkCommandBufferBeginInfo` 误设 `level` 字段（属于 AllocateInfo）
  - `vkCmdSetLineWidth` 传 3 参（真实 2 参）、`vkCmdBindDescriptorSets` 单元素未取址
  - `m_pushConstants`/`kStagingBufferSize` 未声明、内存屏障强转语义错误
  - `getGPUMemoryUsage` 依赖未启用的 `VK_EXT_memory_budget` 扩展（已简化）
  - Vulkan 1.4 SDK 移除弃用 `VK_IMAGE_ASPECT_DEPTH_STENCIL_BIT`（改按位组合）
- **已全部修复并验证**：SanYiRender_d.dll 编译通过、RenderxTests 15/15 通过、SanYiCAD.exe 完整链接。CMake 配置阶段修复 `target_compile_definitions` 在 `add_library` 前调用的问题。
- 遗留：仅 C4100/C4505 无害警告；运行时渲染正确性需在带 HWND 的窗口下真机验证

**与文档差异**：
- ❌ `Renderx/README.md:5` 称 "Vulkan / Metal / Null 仅为类型预留，尚未实现" —— 与代码矛盾（README 已修正）
- ❌ 文档称 "单设备单窗口模型" —— `renderCreateDevice` 实际支持多实例（已修正）
- ⚠️ 本机 Windows 构建 `Vulkan_INCLUDE_DIR:NOTFOUND` → Vulkan 未编入，**无法本地证实编译通过**
- ⚠️ 未披露 gap：
  - Metal 目录无 `.metal` shader 源文件
  - Metal-C++ 绑定依赖 Apple SDK，仓库未内置
  - `Renderx/README.md` 原称 "后续将引入 RenderRuntime / RenderSession" 已被 M1/M2 实现

## 4. SceneEditService + 导入导出 — 大部分真实现，存在坏链

### 已确认真实
- `SceneEditService.cpp` = 507 行真实现：增删改、`applyChangeSet` 事务、`beginBatch/endBatch`、`EntitySnapshotsCommand` 变换、交互会话、锁定图层拒绝
- Import/Export 四服务真实调用链：`ImportService → ImportDispatcher → IImportReader → FileIOManager`

### 坏链与空壳
- ❌ **PDF/BMP/PNG/STEP 导出坏链**：Main 层注册了 writer，但 `FileWriterFactory` 无对应 writer，`exportFile` 必失败；实际可用导出仅 DXF/SVG
- ❌ `ObjExportWriter.cpp`（17 行）显式空壳
- ❌ `File_ImportImage` 只弹对话框，不实际导入
- ❌ DWG 不支持：`FileFormat.h` 枚举无 DWG，libdxfrw 的 DWG 未接线

### 撤销栈口径（文档互相矛盾）
- `框架问题分析DS20260729.md`（2026-07-18）称 "ImportService 走 SceneEditService(Undo)"
- 实际：2D 导入走 `CommitMode::Direct` 直接提交，**不入撤销栈**
- `框架现状与修理计划.md` L49 反而写对了

## 5. 菜单系统 — 未达 CommandCatalog 愿景

**代码事实**：
- 三套菜单管理器并存（2026-08-11 已删其一）：
  - `WorkbenchMenuManager`（2D 现行，1484 行）
  - `MenuManager`（2D 遗留死代码，**2026-08-11 已删除**）
  - `MenuManager3D`（3D 现行）
- **非动态刷新**：File/Edit/View/Help/Modify/Algorithm 菜单全部硬编码，仅 Draw 菜单按 `CommandCatalog` 动态生成
- **2D/3D 职责边界（2026-08-11 复核）**：`refreshXxxMenuForWorkbench()` 系列现仅服务 2D，全部对 `3D` 早退；3D 菜单由 `MenuManager3D` 自理。文件内 `#if BUILD_UI3D` / `CommandCatalog3D` / `MenuActionId3D` 残留已清零
- `CommandCatalog` 存在且较完整（2D 约 118 条 + 3D 约 58 条），但只是查找表，不是唯一生成源
- ✅ 菜单触发链路已统一 `OperationBus::run`（2D 25 处直接接线 + 统一 `logMenuTrigger()` 日志）；部分项绕过（主题/语言/图层对话框直接调用）
- ⚠️ 配置驱动 UI（`rebuildMenusFromConfig` + `UiLayoutBuilder` + JSON）代码已就绪但 `SANYI_ENABLE_CONFIG_DRIVEN_UI=OFF`，运行时仍走硬编码

## 6. 修正过的文档清单

| 文档 | 修正内容 |
|---|---|
| `框架现状与修理计划.md` | 1.2 命名修正；1.3 风险项改写；1.4 差异评估表 WorkbenchWindow 行；5.4 同步说明；5.5 专项状态表；7. 新增 2026-08-10 版本记录 |
| `框架问题分析DS20260729.md` | 2.1 WorkbenchWindow 段落；v1.11 版本记录补正；版本记录新增 2026-08-10 条目 |
| `Renderx/README.md` | 后端状态：Vulkan/Metal/Null 已实现非预留；RenderRuntime/RenderSession 已落地 |
| `RenderX问题分析OpenCodeLing20260805.md` | "单设备单窗口"模型措辞；Vulkan/Metal 骨架→实质实现；平台矩阵更新 |
| `当前架构总览.md` | 变更记录补正（ImportService 撤销口径） |

## 7. 后续跟进（2026-08-10 编译验证后更新）

- ✅ **Vulkan 后端已修复并编译通过**：详见 §3。旧文档 M9"编译通过"断言失实——真实编译暴露 ~70 处错误，已全部修复
- ⏳ **Metal 后端**：Windows 无法编译验证，需在 macOS 上编译（依赖 Apple SDK）；仓库无 `.metal` shader 源文件
- ⏳ **Vulkan 运行时**：编译通过 ≠ 渲染正确，需在真实窗口（HWND）下用 Vulkan 后端跑通渲染帧
- ✅ **CMake 修复**：`Renderx/CMakeLists.txt` 中 `target_compile_definitions` 移至 `add_library` 之后（Vulkan/Metal 分支用 `RENDER_COMPILE_DEFS` 收集）

## 8. 建议的下一步

1. **导出坏链**：PDF/BMP/PNG/STEP 已注册 writer 但工厂无实现 → 补 writer 或移除注册
2. **死代码清理**：✅ 已完成（2026-08-14）——`EditOperations.cpp` 等旧操作层已删除，选择/群组/变换/算法/视图操作已按新框架重写接入
3. **DWG**：确认是否需支持，如支持则补 `FileFormat` 枚举 + libdxfrw 接线
4. **Vulkan 运行时验证**：在应用窗口下切换 Vulkan 后端，验证 swapchain 渲染帧正确
5. **菜单动态化**：评估是否将 File/Edit/View 等菜单迁移到 `CommandCatalog` + 配置驱动
