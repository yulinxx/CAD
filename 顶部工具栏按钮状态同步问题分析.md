# 顶部工具栏按钮状态同步问题分析

## 问题 1：TopToolBar::appendAction 中 toggle action 获取错误

### 问题描述

`TopToolBar::appendAction()` 在从 `CommandActionHub` 查找动作时，只调用了 `editAction()`，没有调用 `toggleEditAction()`。这导致可切换动作（toggle action，如 `Edit_Group`）无法从 Hub 中正确获取，最终退化为创建一个全新的 QAction，与 Hub 管理的动作失去同步。

### 根因分析

**文件**：`/Users/ms/Documents/CAD/UI/2D/Src/UI/TopToolBar/TopToolBar.cpp`
**行号**：129-154（`appendAction` 函数）

当前代码（第 136-144 行）：
```cpp
if (m_commandActionHub)
{
    UI::MenuActionId menuId = actionIdToMenuActionId(actionDef.actionId);
    if (menuId != UI::MenuActionId(-1))
    {
        action = m_commandActionHub->editAction(menuId);  // 只调用了 editAction
        if (action)
        {
            fromHub = true;
        }
    }
    // ...
}
```

对比 `installHubActions()` 的实现（第 71-102 行），它正确地使用了两个函数：
- `addEditAction()` → 调用 `hub->editAction(id)`，用于非 checkable 的编辑动作
- `addToggleAction()` → 调用 `hub->toggleEditAction(id)`，用于 checkable 的切换动作

而 `appendAction()` 只调用了 `editAction()`，完全没有尝试 `toggleEditAction()`。

**验证证据**：
- `CommandActionHub` 中编辑动作存储在 `m_editActions`（第 197 行），通过 `editAction()` 获取
- 切换动作存储在 `m_toggleEditActions`（第 198 行），通过 `toggleEditAction()` 获取
- `Edit_Group` 在 `CommandCatalog.cpp` 第 529 行中 `checkable = true`，因此被放入 `m_toggleEditActions`
- Default 上下文配置中 `Edit_Group` 标记为 `checkable = true`（`UiWorkbench.cpp` 第 1011 行），但通过 `appendAction` 路径添加时找不到 Hub 中的对应 action

### 影响

1. 当通过 `ToolBarContextManager` 动态添加 `Edit_Group` 等切换按钮时，按钮不是 Hub 管理的同一个 QAction 对象
2. 按钮的启用状态（enabled）、选中状态（checked）、文字/图标切换（Group/Ungroup）都不会随 `refreshActionStates` 而更新
3. 点击按钮的行为也可能不同（新创建的 action 没有连接到 `OperationRouting::dispatch`）

### 修复建议

在 `appendAction()` 中，当 `editAction()` 返回 nullptr 时，应再尝试 `toggleEditAction()`。同时需要根据 `actionDef.checkable` 来决定查找顺序，或同时尝试两种方式。

**修改位置**：`/Users/ms/Documents/CAD/UI/2D/Src/UI/TopToolBar/TopToolBar.cpp`，第 136-144 行

**修改方案**：
```cpp
if (m_commandActionHub)
{
    UI::MenuActionId menuId = actionIdToMenuActionId(actionDef.actionId);
    if (menuId != UI::MenuActionId(-1))
    {
        // 优先根据 checkable 属性选择查找路径，两种都尝试确保找到
        if (actionDef.checkable)
        {
            action = m_commandActionHub->toggleEditAction(menuId);
        }
        else
        {
            action = m_commandActionHub->editAction(menuId);
        }
        // 兜底：如果按预期路径没找到，尝试另一个哈希表
        if (!action)
        {
            action = actionDef.checkable
                ? m_commandActionHub->editAction(menuId)
                : m_commandActionHub->toggleEditAction(menuId);
        }
        if (action)
        {
            fromHub = true;
        }
    }
    // ...
}
```

---

## 问题 2：上下文切换后新添加的 actions 是否需要立即刷新启用状态

### 问题描述

当 `ToolBarContextManager::setCurrentContext()` 切换上下文后，`TopToolBar` 会清空并重新添加 actions。新添加的 actions 从 Hub 获取时，其启用状态是否已经是最新的？是否需要在上下文切换后立即调用一次状态刷新？

### 调用链路分析

**上下文切换触发路径**（选择变化时）：

1. `RenderViewport2D::selectionChanged` 信号触发
2. 第一个连接（`setupViewportServices` 中，第 621-626 行）：
   ```cpp
   m_commandHub->refreshActionStates(m_commandHub->captureSnapshot(...));
   ```
   刷新 Hub 中所有 action 的启用状态
3. 后续连接（`createToolbars` 中，第 1155-1163 行）：
   ```cpp
   const ToolBarContext newCtx = determineContextFromSelection();
   if (m_contextManager->currentContext() != newCtx) {
       m_contextManager->setCurrentContext(newCtx);
   }
   ```
   切换上下文 → 更新 TopToolBar actions

### 信号连接顺序

由于 Qt 信号按连接顺序传递，且 `setupViewportServices`（第 416 行调用）在 `createToolbars`（第 444 行调用）之前执行，因此：
- **刷新 Hub 状态的连接先建立** → 先执行
- **上下文切换的连接后建立** → 后执行

因此，当选择变化触发上下文切换时，Hub 的 action 状态已经是最新的，新添加到工具栏的 action 从 Hub 获取时状态正确。

### 潜在问题

尽管常规选择变化路径上状态是同步的，但仍存在以下风险：

1. **非选择变化触发的上下文切换**：如果未来有其他触发上下文切换的路径（如工具切换、模式切换等），此时 Hub 状态可能已过时

2. **初始启动时**：`setCurrentContext(ToolBarContext::Default)`（第 1150 行）在 `rebuildAllActions()`（第 950 行）之后调用，此时 Hub 状态是最新的，无问题

3. **新上下文特有的非 Hub 动作**：对于不在 Hub 中管理的自定义动作（如 `Text_Bold` 等文字编辑按钮），它们的 `enabled` 状态来自 `ToolBarAction` 配置中的静态值，不会随状态变化而刷新

### 修复建议

在 `TopToolBar::setActions()` 完成动作设置后，如果 Hub 存在，则主动触发一次状态刷新，作为防御性措施确保状态一致。

**修改位置**：`/Users/ms/Documents/CAD/UI/2D/Src/UI/TopToolBar/TopToolBar.cpp`，第 109-122 行

**修改方案**：
```cpp
void TopToolBar::setActions(const QList<ToolBarAction>& actions)
{
    clearHubActions();

    if (actions.isEmpty())
    {
        return;
    }

    for (const auto& actionDef : actions)
    {
        appendAction(actionDef);
    }

    // 新增：设置完所有动作后，从 Hub 刷新一次启用状态
    if (m_commandActionHub)
    {
        m_commandActionHub->refreshActionStates(
            m_commandActionHub->captureSnapshot(m_commandActionHub->mainWindow()));
    }
}
```

**注意**：此方案的前提是 `CommandActionHub::refreshActionStates` 会刷新所有 Hub 管理的 action（包括当前不在工具栏上的），而目前的实现确实如此（遍历整个 `CommandCatalog`）。

---

## 问题 3：setupViewportServices 与 createToolbars 的调用顺序

### 问题描述

`Workbench2D::attachToWindow()` 中，`setupViewportServices()` 在 `createToolbars()` 之前调用。`setupViewportServices()` 内连接了 `selectionChanged` 信号到 `m_commandHub->refreshActionStates()`，但此时 `m_commandHub` 尚未创建。需要确认这是否会导致问题。

### 调用顺序

**文件**：`/Users/ms/Documents/CAD/Main/Src/UI/Workbench/UiWorkbench.cpp`

`attachToWindow()` 中的调用顺序（第 400-465 行）：
1. 第 416 行：`setupViewportServices(vp, window)`
   - 第 621-626 行：连接 `selectionChanged` 到刷新 Hub 状态的 lambda
   - **此时 `m_commandHub` 为 nullptr**
2. 第 444 行：`createToolbars(window)`
   - 第 841 行：`m_commandHub = std::make_unique<CommandHub>()`
   - 第 950 行：`m_commandHub->rebuildAllActions()`
   - 第 955 行：`m_topToolBar->setCommandActionHub(m_commandHub.get())`

### 现状分析

`selectionChanged` 信号的 lambda 中有保护判断（第 622 行）：
```cpp
QObject::connect(vp, &RenderViewport2D::selectionChanged, this, [this]() {
    if (m_commandHub)  // 空指针保护
    {
        m_commandHub->refreshActionStates(m_commandHub->captureSnapshot(m_commandHub->mainWindow()));
    }
});
```

由于有 `if (m_commandHub)` 保护，不会发生空指针崩溃。

### 潜在问题

1. **早期 selectionChanged 信号丢失**：在 `setupViewportServices` 之后、`createToolbars` 之前，如果视口触发了 `selectionChanged` 信号（例如 `vp->initializeTools()` 或 `vp->setActiveTool()` 过程中），该信号会被忽略，因为 `m_commandHub` 仍为 nullptr

2. **初始状态依赖 rebuildAllActions**：幸运的是，`createToolbars` 中的 `rebuildAllActions()`（第 950 行）会调用 `captureSnapshot()` 获取当前选择状态，所以初始按钮状态是正确的

3. **代码可读性和维护性**：连接建立时目标对象（m_commandHub）不存在，依赖后续赋值才能生效，这种模式容易在未来修改时引入 bug

### 修复建议

将 `selectionChanged` 到 `m_commandHub` 的刷新连接从 `setupViewportServices` 移到 `createToolbars` 中 `m_commandHub` 创建之后，确保连接建立时目标已存在。

**修改位置**：`/Users/ms/Documents/CAD/Main/Src/UI/Workbench/UiWorkbench.cpp`

1. **删除** `setupViewportServices` 中第 620-626 行的连接
2. **添加**到 `createToolbars` 中 `m_commandHub` 创建并设置完 providers 之后（建议在第 950 行 `rebuildAllActions()` 之后）

**修改方案**（在 `createToolbars` 中，`rebuildAllActions` 之后添加）：
```cpp
m_commandHub->rebuildAllActions();

// 选择变化 → 刷新顶部工具栏动作启用状态
if (m_viewport)
{
    QObject::connect(m_viewport, &RenderViewport2D::selectionChanged, this, [this]() {
        if (m_commandHub)
        {
            m_commandHub->refreshActionStates(m_commandHub->captureSnapshot(m_commandHub->mainWindow()));
        }
    });
}
```

这样做的好处：
- 连接建立时 `m_commandHub` 已经存在，语义更清晰
- 避免了空指针保护的冗余判断（虽然保留也无害）
- 代码组织更合理：Hub 相关的信号连接放在 Hub 创建的地方

---

## 问题 4：CommandActionHub::captureSnapshot 中选择计数的获取逻辑

### 问题描述

检查 `CommandActionHub::captureSnapshot()` 中选择计数（selectionCount / hasSelection）的获取逻辑是否正确，是否存在状态不一致的风险。

### 当前实现

**文件**：`/Users/ms/Documents/CAD/UI/2D/Src/Operation/CommandActionHubActions2D.cpp`
**行号**：487-492

```cpp
// 选择状态由注入的计数提供器采集（避免 Hub 直接依赖 ISelectionService）
if (m_selectionCountProvider)
{
    snapshot.selectionCount = m_selectionCountProvider();
    snapshot.hasSelection = snapshot.selectionCount > 0;
}
```

### 分析

#### 4.1 基础逻辑正确性

`hasSelection` 由 `selectionCount > 0` 推导而来，两者来自同一次 provider 调用，**在 captureSnapshot 内部是一致的**。这部分逻辑没有问题。

#### 4.2 数据源不一致问题（关键隐患）

**问题在于不同 provider 使用了不同的选择数据源**，可能导致快照内部状态不一致：

- **选择计数 provider**（`UiWorkbench.cpp` 第 844-852 行）：使用 `ISelectionService`
  ```cpp
  selectionService->visitSelectedIds(&countSelectedIds, &count);
  ```

- **分组切换 provider**（`UiWorkbench.cpp` 第 897-935 行）：使用 `SceneManager::getSelectedEntities()`
  ```cpp
  const auto selected = scene->getSelectedEntities();
  ```

- **选择锁定 provider**（`UiWorkbench.cpp` 第 854-895 行）：使用 `ISelectionService`
  ```cpp
  selectionService->visitSelectedIds(...)
  ```

如果 `ISelectionService` 和 `SceneManager::getSelectedEntities()` 在某些时序下不同步，会出现：
- `snapshot.hasSelection = false`（selectionService 报告 0 个选中）
- `snapshot.groupToggleEnabled = true`（scene 报告有选中项且不在锁定层）

或者相反的情况。这种不一致会导致按钮状态混乱。

#### 4.3 GroupToggleState.enabled 的判定逻辑问题

`groupToggleProvider` 中（第 932-933 行）：
```cpp
state.on = hasGroup;
state.enabled = !onLockedLayer;
```

`state.enabled` 只判断了是否在锁定图层，**没有判断是否有选中项**。虽然函数开头有 `if (selected.empty()) return state;` 的短路返回（此时 enabled 为默认 false），但逻辑上不够清晰：如果选中项都在锁定层，`onLockedLayer = true`，`enabled = false`，这是正确的；但如果没有选中项，靠的是 `selected.empty()` 的提前返回来保持 `enabled = false`。

相比之下，`CommandUiSnapshot` 中有明确的 `hasSelection` 字段，而分组切换的启用状态应该同时依赖"有选中项"和"选中项不在锁定层"两个条件。

### 修复建议

#### 方案 A：统一选择数据源（推荐）

将 `groupToggleProvider` 改为使用 `ISelectionService` 而非 `scene->getSelectedEntities()`，与选择计数 provider 保持一致，从根源上消除不一致。

**修改位置**：`/Users/ms/Documents/CAD/Main/Src/UI/Workbench/UiWorkbench.cpp`，第 897-935 行

核心思路：通过 `selectionService->visitSelectedIds` 遍历选中项 ID，再从 scene 查找实体进行判断，而不是直接从 scene 获取选中列表。

#### 方案 B：在 captureSnapshot 中增加一致性校验

如果数据源暂时无法统一，至少在 `captureSnapshot` 中确保 `groupToggleEnabled` 与 `hasSelection` 逻辑一致：

**修改位置**：`/Users/ms/Documents/CAD/UI/2D/Src/Operation/CommandActionHubActions2D.cpp`，第 500-506 行

```cpp
// 分组切换状态（Group/Ungroup 按钮实时刷新）
if (m_groupToggleProvider)
{
    const GroupToggleState gs = m_groupToggleProvider();
    snapshot.groupToggleOn = gs.on;
    // 确保 enabled 与 hasSelection 一致：没有选中项时一定不可用
    snapshot.groupToggleEnabled = gs.enabled && snapshot.hasSelection;
}
```

#### 方案 C：明确 GroupToggleState.enabled 的语义

在 `groupToggleProvider` 中增加明确的非空判断，使逻辑更清晰：

```cpp
state.enabled = !selected.empty() && !onLockedLayer;
```

虽然 `selected.empty()` 的提前返回已经保证了空选择时 state.enabled 为 false，但显式写出逻辑更清晰、更易维护。

---

## 总结

| 问题 | 严重程度 | 位置 | 核心原因 |
|------|---------|------|---------|
| 1. toggle action 获取错误 | 高 | `TopToolBar.cpp:139` | `appendAction` 只调用 `editAction()`，未调用 `toggleEditAction()` |
| 2. 上下文切换后状态刷新 | 中 | `TopToolBar.cpp:setActions` | 新添动作依赖调用前的 Hub 状态，缺乏防御性刷新 |
| 3. 调用顺序导致连接时 Hub 未创建 | 低 | `UiWorkbench.cpp:416/444` | `setupViewportServices` 中连接信号时 `m_commandHub` 为 nullptr（有空指针保护，无崩溃风险） |
| 4. 选择计数数据源不一致 | 中 | `UiWorkbench.cpp:897-935` | `groupToggleProvider` 使用 `scene->getSelectedEntities()`，而 selectionCount 使用 `selectionService`，可能不一致 |
